[project]
name = "comfyui-id-lora-gguf"
version = "0.1.0"
description = "ComfyUI nodes for GGUF-backed ID-LoRA-2.3 audio+video generation with speaker identity transfer"
requires-python = ">=3.10"
dependencies = [
    "ltx-core",
    "ltx-pipelines",
    "torch",
    "torchaudio",
]
